pub fn web_authentication_login() -> Vec<u8> {
    println!("Got request");
    let response = vec![];
    response
}

pub fn web_authentication_logout(
    _domain: String,
    _client_id: String,
    _return_to: Option<String>,
) -> Vec<u8> {
    let response = vec![];
    response
}
